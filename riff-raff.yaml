regions:
- eu-west-1
stacks:
- subscriptions
deployments:
  digital-subscription-authorisation:
    type: aws-lambda
    parameters:
      bucket: subscriptions-dist
      prefixStack: false
      fileName: 'digital-subscription-authorisation.zip'
      functionNames:
      - DigitalSubAuth-
  digital-subscription-authorisation: #it looks like these two cannot have the same name!! see how to do this!
    type: aws-lambda
    parameters:
      bucket: subscriptions-dist
      prefixStack: false
      fileName: 'digital-subscription-authorisation.jar'
      functionNames:
      - DigitalSubs-
